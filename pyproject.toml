[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "parietal_patterns"
version = "0.1.0"

[tool.setuptools]
package-dir = {"" = ".."}
packages = ["parietal_patterns", "parietal_patterns.utils"]
